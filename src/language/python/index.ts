import { LanguageSpecification } from 'qvog-engine';

/**
 * Specification for Python language.
 * 
 * This is currently empty, as the Python language is not yet implemented.
 * 
 * @category Language
 */
const PythonSpecification: LanguageSpecification = {
    valueRules: [],
    typeRules: [],
};

export { PythonSpecification };
